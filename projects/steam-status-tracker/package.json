{
  "name": "steam-status-tracker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Personal digital profile and Steam activity tracker, published with GitHub Pages.",
  "scripts": {
    "update": "node scripts/update-steam-status.mjs",
    "validate": "node scripts/validate-project.mjs",
    "check": "node scripts/validate-project.mjs && node --test tests/*.test.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
