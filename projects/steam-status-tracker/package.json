{
  "name": "steam-status-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A lightweight web application for checking public Steam profile status.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file=.env --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
